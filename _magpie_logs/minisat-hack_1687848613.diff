--- before: core/Solver.cc
+++ after: core/Solver.cc
@@ -691,6 +691,7 @@
     assert(decisionLevel() == 0);
 
     if (!ok || propagate() != CRef_Undef)/*auto*/{
+        cancelUntil(0);
         return ok = false;
 }/*auto*/
 
